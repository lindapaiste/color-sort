/**
 * can concatenate boxIndex with ball/row/column Index
 * and use spilt to get back inputs
 *
 * or can use numeric ids 1 to n
 * and divide by balls per box to get box id
 *
 * but if each knows its own id and slots are the drop zones rather than boxes,
 * I probably never even need to know the boxIndex
 */
